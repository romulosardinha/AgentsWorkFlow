---
description: Explica o que o código faz em português, sem modificar
argument-hint: <caminho-do-arquivo>
---

Leia o arquivo `$1` e explique, **em português claro**:

1. **Propósito geral** — em 1-2 frases, qual o objetivo do arquivo.
2. **Estrutura** — quais funções/classes principais e o que cada uma faz (1 linha cada).
3. **Fluxo principal** — se rodar este código, o que acontece de cima a baixo.
4. **Pontos de atenção** — partes onde a lógica pode confundir, suposições implícitas, dependências externas.

Formato: markdown com cabeçalhos. Não inclua o código original (o usuário já tem). Não modifique o arquivo.

Se o arquivo tiver mais de 300 linhas, foque nas funções públicas e mencione que ignorou helpers internos.
